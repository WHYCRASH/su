package io.github.mangi.eta.core

/** Log identity shared by the app logger and the file sink. */
internal object ModuleConfig {
    const val TAG = "su"
    const val HOT_PATH_LOG_WINDOW_MS = 60_000L
}
